[package]
name = "predictor"
version = "0.1.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
helix = "0.6.4"
chrono = "0.4"

lazy_static = "0.2"

serde = "1.0"
serde_derive = "1.0"
serde_json = "1.0"

lru-cache = "~0.1"

rand = "0.3.0"

libc = "0.2.22"